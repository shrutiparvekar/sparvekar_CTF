The binary bomb is here to reveal the final flag for you if you can provide all the secrets it asks for.
It uses a special algorithm to decrypt the final flag. Your task is to find the key required for this decryption.
The key is hidden in the only human readable file inside another directory in the current folder.
