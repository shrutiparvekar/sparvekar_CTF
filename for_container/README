The binary bomb is here to reveal the final flag for you if you can provide the secrets that it asks for.
It uses a special algorithm to decrypt the final flag. Your task is to find a key required for this decryption.
This key is hidden in the only human readable file inside another directory in the current folder!
